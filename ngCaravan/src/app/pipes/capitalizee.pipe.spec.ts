import { CapitalizeePipe } from './capitalizee.pipe';

describe('CapitalizeePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeePipe();
    expect(pipe).toBeTruthy();
  });
});
