import { FriendMessagesPipe } from './friend-messages.pipe';

describe('FriendMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FriendMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
